"""
Prompt 模板 — 单图/批量报告的 DS 提示词

内容分类器已集成（MobileCLIP2-S0 零样本分类），管线输出中新增 content_category
与 is_super_oversight_domain 字段。当 is_super_oversight_domain==true 时，系统
确认图像内容属于超监管领域（战争/暴恐/枪械/暴力等），此时可在研判中提及具体
内容类别；否则仅使用「高置信伪造」口径，不声称检测到任何语义类别。
"""

# ==============================================================================
# System Prompt (通用)
# ==============================================================================

SYSTEM_PROMPT = """你是一位资深的 AIGC 图像取证分析专家。你会收到 TraceGuard 检测系统的结构化输出数据（JSON），请基于数据生成专业的取证研判意见。

要求：
1. 使用中文输出，语气客观、专业、简洁
2. 明确指出证据的强度和局限性
3. 不要编造数据中不存在的信息
4. 对于高风险条目（risk_level=high），明确建议人工复核
5. 对于高置信伪造条目（fake_prob≥0.9 且 risk_level=high），强调其加急复核的优先级
6. 严格按要求的格式输出，不要遗漏任何段落
7. 本系统集成了轻量级内容类别分类器（CLIP 零样本），当 is_super_oversight_domain==true 时可确认图像内容属于超监管领域（战争/暴恐/枪械/暴力等），此时可在研判中提及具体内容类别与加急处置建议；若 is_super_oversight_domain==false，则仅以「高置信伪造」口径撰写，不声称检测到任何语义内容类别"""


# ==============================================================================
# 单图报告 User Prompt
# ==============================================================================

SINGLE_REPORT_PROMPT = """请基于以下 TraceGuard 单图检测的结构化数据，生成专业的取证研判意见。

## 检测数据

{result_json}

## 输出格式要求

请严格按照以下三段格式输出：

[综合研判意见]
（2-3 段文字，包含：整体判定结论及置信度评价、关键风险因素解读、建议处理措施。如为高置信伪造条目应特别强调其加急复核优先级。）

[维度解读]
（对以下五个风险维度逐一给出简短解读，每维度一句话，说明该维度的分数意味着什么：
- 检测置信度 (fake_prob, 权重 0.50)
- 伪影强度 (artifact_intensity, 权重 0.25)
- 篡改面积比 (tamper_area, 权重 0.10)
- 区域数量 (region_count, 权重 0.05)
- 一致性 (consistency, 权重 0.10)）

[区域解读]
（对可疑区域的分布特征进行分析：区域数量是多还是少、面积是大还是小、分布是集中还是分散、局部风险分是否一致等，并说明这些特征对判定结论的支持程度）"""


# ==============================================================================
# 批量报告 User Prompt
# ==============================================================================

BATCH_REPORT_PROMPT = """请基于以下 TraceGuard 批量检测的结构化数据，生成专业的批量取证研判意见。

## 批次统计

{batch_stats_json}

## 高风险条目摘要

{high_risk_summary}

## 输出格式要求

请严格按照以下三段格式输出：

[批次整体评估]
（2-3 段文字，包含：批次整体情况的评价、伪造比例和风险分布的解读、是否存在集中性的可疑模式。如有高置信伪造条目应特别指出。若统计中含 screening_context，说明这是筛查后的待处置子集，评估时应以"待处置条目"口径表述，不要将伪造比例解读为原始批次比例。）

[高风险优先级排序]
（将高风险条目按紧急程度排序，每项包含：序号、文件名简短标识、排序理由（一句话）。格式如下：
1. [文件标识] — 排序理由
2. [文件标识] — 排序理由
...）

[逐图复核建议]
（为每一条高风险条目提供针对性的复核建议，每条建议1-2句话，指出该图需要重点关注的方面。格式如下：
#1: 复核建议文字
#2: 复核建议文字
...）"""


# ==============================================================================
# Fallback 文本 (LLM 不可用时使用)
# ==============================================================================

FALLBACK_SINGLE_OPINION = """[综合研判意见]
该图像已完成自动化检测分析。由于大语言模型服务暂时不可用，本报告仅包含基于规则模板的自动研判。

检测系统已输出完整的量化指标（伪造概率、风险维度分数、可疑区域坐标），建议审核人员依据上述数据进行人工评判。如有疑问，请参考「详细解释」段落中的自动生成文本。

[维度解读]
各维度分数为系统自动计算的客观指标，具体数值请参见「风险维度详情」表格。由于 LLM 服务不可用，无法生成逐维度自然语言解读。

[区域解读]
可疑区域分布信息请参见「可疑区域列表」表格。由于 LLM 服务不可用，无法生成分布特征的自然语言分析。"""

FALLBACK_BATCH_OVERVIEW = """[批次整体评估]
本批次已完成自动化批量检测。由于大语言模型服务暂时不可用，本报告仅包含基于规则模板的自动汇总。

请审核人员根据「批次统计概览」和「汇总图表」中的量化数据进行人工评判。高风险条目已在「逐条结果汇总表」中以红色底色标注。

[高风险优先级排序]
由于 LLM 服务不可用，无法生成智能优先级排序。建议按综合风险分 (risk_score) 从高到低依次审核。

[逐图复核建议]
由于 LLM 服务不可用，无法生成逐图复核建议。请审核人员结合热力图和维度分析逐图判断。"""
