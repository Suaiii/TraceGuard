# Risk calibration evidence

`facebook_balanced_200_summary.json` records a deterministic 60/40 stratified calibration/holdout run on 100 real and 100 fake Facebook-derived images covering nine fake generators. Boundaries were selected on the calibration split and reported only on the untouched holdout split. The result is a source-specific threshold candidate, not a production-wide calibration.
